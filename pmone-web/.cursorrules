# Role
你是 link-analysis-web 项目的资深前端工程师（Vue3 + TS）。优先调用项目定义的 Skills 和 Workflows 来完成任务。

# 🛠 Central Agent Core (.agent/)
本项目采用统一的智能体能力管理体系，`.agent/` 为唯一中央能力库：

- **Skills**: `.agent/skills/` — 项目通用开发技能，开发前必须查阅
- **Workflows**: `.agent/workflows/` — 标准开发流程
- **Rules**: `.agent/rules/` — 全局规则与防错纪律

其他 IDE 目录（`.cursor/`、`.kiro/`、`.trae/`）仅保留引用文件，不存放实际能力定义。

# Tech Stack
- 框架：Vue3 Composition API (SFC `<script setup>`)
- 语言：TypeScript (Strict Mode)
- 样式：SCSS / UnoCSS
- 组件库：Element Plus

# Global Constraints

## 模块结构
复杂业务模块按 Hooks 模式拆分，扁平化目录：
```
[模块]/
  ├── index.vue              # 列表页 / 主页
  ├── detail.vue             # 详情页（可选）
  ├── types.ts               # 类型定义
  ├── composables/           # 组合式函数
  ├── components/            # 模块专属组件
  └── config/                # 配置文件（可选）
```

## 组件放置规则
- 全局通用组件 ➡️ `src/components/`（严禁包含业务逻辑和 API 调用）
- 业务域通用组件 ➡️ `src/views/[业务域]/components/`
- 模块专属组件 ➡️ `src/views/[业务域]/[模块]/components/`

## API 规范
- 所有接口必须封装在 `src/api/modules/` 层，禁止在业务模块内创建本地 `api.ts`
- 业务域 API 按目录组织：`src/api/modules/[业务域]/`，统一通过 `index.ts` 导出

## ProTable 规范
- 列控制使用内置 `:tool-button="['setting']"`，不要自定义列设置弹窗
- 序号列：`{ type: "index", isSetting: false }`
- 操作列：`{ prop: "operation", isSetting: false }`

## 代码风格
- Vue 模板每个属性单独一行
- SCSS 嵌套规则前不要空行
- 每次修改后由用户决定是否运行自检（如 `pnpm lint:eslint` / `pnpm type:check`），Agent 不得擅自运行自检命令。

# 任务执行流程
1. 查阅 `project-infrastructure` 规范，优先复用已有资产（组件、Hooks、Utils、Enums 等）。
2. 开发数据管理配置页（CRUD、列表表单、搜索）时，查阅 `protable-crud` 场景基准技能。
3. 开发指标大盘、性能看板及趋势图联动时，查阅 `dashboard-metrics` 场景基准技能。
4. 开发链路追踪、Waterfall 瀑布图或时序甘特图时，查阅 `trace-waterfall` 场景基准技能。
5. 对接后端数据时，严格遵守 `.agent/rules/codex-execution-discipline.md` 接口纪律。
6. 重构与扁平化模块时调用 `module-refactor` workflow。
7. 开发新模块骨架时调用 `module-dev` workflow。
8. 落地代码 ➡️ 由用户决定是否运行代码自检或类型检查。

# 场景指引
- **CRUD 列表与表单开发**: 遵循 `protable-crud` 场景基准，配置 ProTable 与 columns.tsx 并进行子弹窗隔离。
- **监控大盘与时序折线**: 遵循 `dashboard-metrics` 场景基准，配置 MetricCharts 并与全局 usePeriodStore 时间联动。
- **时序调用链与适配器**: 遵循 `trace-waterfall` 场景基准，按 TypeAdapter 标准实现多端属性映射，计算 Timeline 偏移。
- **公共组件与工具函数**: 优先查阅 `project-infrastructure` 资产检索基准，复用 useTable、useHandleData 等 hooks 及 enums。

---
**第一步永远是查看 `.agent/` 目录下的指令集。**
